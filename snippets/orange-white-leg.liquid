{% comment %}
  Section: Orange Wide Leg Card
  Modal-friendly product detail card with editable settings, designed to be fetched into a modal.
{% endcomment %}

{%- assign close_icon_url = section.settings.close_icon != blank 
  ? section.settings.close_icon | image_url: width: 22 
  : 'img_group_1000008134.svg' | asset_url -%}

{%- assign dropdown_arrow_url = section.settings.dropdown_arrow != blank 
  ? section.settings.dropdown_arrow | image_url: width: 24 
  : 'img_arrow_down.svg' | asset_url -%}

{%- assign cart_arrow_url = section.settings.cart_arrow != blank 
  ? section.settings.cart_arrow | image_url: width: 48 
  : 'img_line_15_white_a700.svg' | asset_url -%}

{%- assign product_image_url = section.settings.product_image != blank 
  ? section.settings.product_image | image_url: width: 800 
  : 'img_image_822.png' | asset_url -%}

<style>
  /* Scoped namespace: owl- (Orange Wide Leg) */
  .owl-container { background:#fff; box-shadow:0 2px 4px #00000033; width:100%; max-width:400px; margin:0 auto; padding:12px 16px 24px 16px; font-family:'Jost', Arial, sans-serif; }
  .owl-content { display:flex; flex-direction:column; align-items:center; width:100%; }
  .owl-header { display:flex; flex-direction:column; align-items:center; width:100%; margin-bottom:16px; }
  .owl-image { width:45%; height:159px; object-fit:cover; margin-bottom:-2px; margin-right:16px; }
  .owl-info { display:flex; flex-direction:column; align-items:flex-start; flex:1; gap:16px; width:100%; }
  .owl-close { width:16px; height:11px; align-self:flex-end; margin-bottom:16px; cursor:pointer; }

  .owl-details { display:flex; flex-direction:column; align-items:flex-start; width:100%; gap:10px; }
  .owl-title { font-size:14px; font-weight:300; line-height:18px; color:#000; }
  .owl-price { font-size:14px; font-family:'Lustria', serif; font-weight:400; line-height:16px; color:#000; }
  .owl-desc  { font-size:12px; font-weight:300; line-height:14px; color:#000; width:100%; }

  .owl-color { display:flex; flex-direction:column; align-items:flex-start; width:100%; max-width:272px; gap:2px; margin-top:16px; }
  .owl-label { font-size:12px; font-weight:400; line-height:16px; color:#333; }
  .owl-color-row { display:flex; align-items:center; width:100%; border:1px solid #000; }
  .owl-color-opt { padding-right:79px; display:flex; align-items:center; cursor:pointer; transition:all .3s ease; }
  .owl-color-opt:hover { opacity:.8; }
  .owl-swatch { width:7px; height:32px; border:1px solid #000; }
  .owl-swatch--primary { background-color: {{ section.settings.color_primary | default: '#FFFFFF' }} !important; border-color:#000 !important; }
  .owl-swatch--secondary { background-color: {{ section.settings.color_secondary | default: '#000000' }} !important; border-color:#000 !important; }
  .owl-color-text { font-size:16px; font-weight:400; line-height:20px; color:#000; margin-left:8px; text-transform:capitalize; }
  .owl-color-opt.selected { background:#000; }
  .owl-color-opt.selected .owl-color-text { color:#fff; }

  .owl-size { display:flex; flex-direction:column; align-items:flex-start; width:100%; max-width:272px; gap:2px; margin-top:8px; }
  .owl-sizebar { position:relative; width:100%; border:1px solid #000; display:flex; align-items:center; cursor:pointer; user-select:none; }
  .owl-size-text { padding-left:3%; font-size:14px; font-weight:400; line-height:18px; color:#000; margin:8px 0; flex:1; }
  .owl-size-ctrl { display:inline-flex; align-items:center; gap:8px; pointer-events:none; padding:0 8px; }
  .owl-size-line { width:1px; height:30px; background:#000; }
  .owl-arrow { width:12px; height:6px; transition:transform .25s; }

  .owl-menu { position:absolute; top:110%; left:0; right:0; max-height:96px; overflow-y:auto; border:1px solid #000; background:#fff; box-shadow:0 6px 12px #0002; margin:6px 0 0; padding:0; list-style:none; display:none; z-index:1000; scrollbar-width:none; -ms-overflow-style:none; }
  .owl-menu::-webkit-scrollbar { display:none; }
  .owl-menu.show { display:block; }
  .owl-opt { padding:8px 12px; font-family:'Jost', Arial, sans-serif; font-size:14px; line-height:18px; border-bottom:1px solid #eee; cursor:pointer; }
  .owl-opt:last-child { border-bottom:none; }
  .owl-opt:hover, .owl-opt.selected { background:#000; color:#fff; }

  .owl-cta { display:flex; justify-content:flex-end; align-items:center; width:100%; max-width:272px; background:#000; padding:8px 30px; margin-top:40px; cursor:pointer; transition:all .3s ease; border:0; }
  .owl-cta:hover { background:#333; transform:translateY(-2px); }
  .owl-cta:active { transform:translateY(0); background:#555; }
  .owl-cta-text { font-size:14px; font-weight:400; line-height:18px; text-transform:uppercase; color:#fff; }
  .owl-cta-arrow { width:32px; height:1px; margin-left:28px; }

  @media (min-width:640px) {
    .owl-header { flex-direction:row; align-items:end; }
    .owl-image { height:159px; width:45%; object-fit:cover; margin-bottom:-2px; margin-right:16px; }
    .owl-price { font-size:17px; }
    .owl-container { max-width:282px; }
    .owl-desc { font-size:14px; font-weight:200; }
    .owl-color { margin-top:-7px; }
  }

  @media (min-width:768px) {
    .owl-title { font-size:16px; line-height:24px; }
    .owl-price { font-size:16px; line-height:21px; }
    .owl-desc { font-size:13px; line-height:15px; }
    .owl-image { height:184px; margin-left:8px; }
    .owl-cta { margin-top:50px; }
    .owl-color-opt { padding-right:90px; }
    .owl-cta-text { font-size:16px; line-height:24px; }
    .owl-swatch { height:40px; }
    .owl-size-text { font-size:16px; line-height:24px; padding-left:4%; }
    .owl-size-line { height:38px; }
  }

  @media (min-width:1024px) {
    .owl-color-opt { padding-right:100px; }
    .owl-container { max-width:350px; padding:20px 32px 40px 32px; }
    .owl-desc { font-size:12px !important; }
    .owl-image { height:152px !important; margin-left:5px !important; }
  }

  @media (min-width:0px) and (max-width:639px) {
    .owl-header { flex-direction:row; }
    .owl-details { padding-top:31px; }
    .owl-container { max-width:330px !important; position:relative; }
    .owl-image { width:123px !important; height:122px !important; margin-bottom:-34px !important; padding-left:10px !important; }
    .owl-info { padding-left:7px !important; }
    .owl-desc { font-size:13px !important; }
    .owl-color { margin-top:0 !important; }
    .owl-close { position:absolute; top:10px; right:10px; z-index:10; }
  }
</style>

<div class="owl-container">
  <div class="owl-content">
    <div class="owl-header">
      <img src="{{ product_image_url }}" alt="{{ section.settings.product_image.alt | escape | default: 'Product image' }}" class="owl-image" />
      <div class="owl-info">
        <!-- Keep .close-button so the landing modal can bind to it -->
        <img src="{{ close_icon_url }}" alt="Close" class="owl-close close-button" />
        <div class="owl-details">
          <h1 class="owl-title">{{ section.settings.title | default: 'Orange Wide Leg' }}</h1>
          <p class="owl-price">{{ section.settings.price | default: '980,00€' }}</p>
          <p class="owl-desc">{{ section.settings.description | default: 'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.' }}</p>
        </div>
      </div>
    </div>

    <div class="owl-color">
      <label class="owl-label">{{ section.settings.color_label | default: 'Color' }}</label>
      <div class="owl-color-row">
        <div class="owl-color-opt">
          <div class="owl-swatch owl-swatch--primary"></div>
          <span class="owl-color-text">{{ section.settings.color_primary_label | default: 'White' }}</span>
        </div>
        <div class="owl-color-opt" style="flex:1;">
          <div class="owl-swatch owl-swatch--secondary"></div>
          <span class="owl-color-text">{{ section.settings.color_secondary_label | default: 'Black' }}</span>
        </div>
      </div>
    </div>

    <div class="owl-size">
      <label class="owl-label">{{ section.settings.size_label | default: 'Size' }}</label>
      <div class="owl-sizebar" id="owlSizeBar" tabindex="0" aria-haspopup="listbox" aria-expanded="false">
        <span class="owl-size-text" id="owlSizeText">{{ section.settings.size_prompt | default: 'Choose your size' }}</span>
        <div class="owl-size-ctrl">
          <div class="owl-size-line"></div>
          <img src="{{ dropdown_arrow_url }}" alt="toggle" class="owl-arrow" id="owlArrow">
        </div>
        <ul class="owl-menu" id="owlMenu" role="listbox" aria-label="Size options">
          {%- assign size_list = section.settings.sizes | default: 'XS,S,M,L,XL' | split: ',' -%}
          {%- for s in size_list -%}
            {%- assign s_trim = s | strip -%}
            <li class="owl-opt" role="option" aria-selected="false">{{ s_trim }}</li>
          {%- endfor -%}
        </ul>
      </div>
    </div>

    <button class="owl-cta" type="button" id="owlAddToCart">
      <span class="owl-cta-text">{{ section.settings.cta_label | default: 'Add to cart' }}</span>
      <img src="{{ cart_arrow_url }}" alt="Arrow" class="owl-cta-arrow" />
    </button>
  </div>
</div>

<script>
  (function() {
    // Size dropdown
    var bar = document.getElementById('owlSizeBar');
    var menu = document.getElementById('owlMenu');
    var label = document.getElementById('owlSizeText');
    var arrow = document.getElementById('owlArrow');
    var open = false;

    function openMenu() { open = true; menu.classList.add('show'); arrow.style.transform = 'rotate(180deg)'; bar.setAttribute('aria-expanded', 'true'); }
    function closeMenu() { open = false; menu.classList.remove('show'); arrow.style.transform = 'rotate(0deg)'; bar.setAttribute('aria-expanded', 'false'); }
    function toggleMenu() { open ? closeMenu() : openMenu(); }

    if (bar) {
      bar.addEventListener('click', function(e){ e.stopPropagation(); toggleMenu(); });
      window.addEventListener('click', function(){ if (open) closeMenu(); });
      window.addEventListener('keydown', function(e){
        if (e.key === 'Escape' && open) closeMenu();
        if ((e.key === 'Enter' || e.key === ' ') && document.activeElement === bar) { e.preventDefault(); toggleMenu(); }
      });
    }

    if (menu) {
      menu.querySelectorAll('.owl-opt').forEach(function(opt){
        opt.addEventListener('click', function(e){
          e.stopPropagation();
          label.textContent = opt.textContent;
          menu.querySelectorAll('.owl-opt').forEach(function(o){ o.classList.remove('selected'); o.setAttribute('aria-selected','false'); });
          opt.classList.add('selected');
          opt.setAttribute('aria-selected','true');
          closeMenu();
        });
      });
    }

    // Color selection
    var colorOpts = document.querySelectorAll('.owl-color-opt');
    colorOpts.forEach(function(opt){
      opt.addEventListener('click', function(){
        colorOpts.forEach(function(o){ o.classList.remove('selected'); });
        opt.classList.add('selected');
      });
    });

    // Add to cart placeholder: emit event for host page handler
    var addBtn = document.getElementById('owlAddToCart');
    if (addBtn) {
      addBtn.addEventListener('click', function(){
        var evt = new CustomEvent('owl:add-to-cart', {
          detail: {
            title: '{{ section.settings.title | escape }}',
            price: '{{ section.settings.price | escape }}',
            size: label ? label.textContent : null
          }
        });
        window.dispatchEvent(evt);
      });
    }
  })();
</script>

{% schema %}
{
  "name": "Orange Wide Leg Card",
  "settings": [
    { "type": "image_picker", "id": "product_image", "label": "Product image" },
    { "type": "text", "id": "title", "label": "Title", "default": "Orange Wide Leg" },
    { "type": "text", "id": "price", "label": "Price", "default": "980,00€" },
    { "type": "textarea", "id": "description", "label": "Description", "default": "This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief." },

    { "type": "image_picker", "id": "close_icon", "label": "Close icon" },
    { "type": "image_picker", "id": "dropdown_arrow", "label": "Dropdown arrow icon" },
    { "type": "image_picker", "id": "cart_arrow", "label": "Cart arrow icon" },

    { "type": "text", "id": "color_label", "label": "Color label", "default": "Color" },
    { "type": "text", "id": "color_primary_label", "label": "Primary color label", "default": "White" },
    { "type": "color", "id": "color_primary", "label": "Primary color swatch", "default": "#FFFFFF" },
    { "type": "text", "id": "color_secondary_label", "label": "Secondary color label", "default": "Black" },
    { "type": "color", "id": "color_secondary", "label": "Secondary color swatch", "default": "#000000" },

    { "type": "text", "id": "size_label", "label": "Size label", "default": "Size" },
    { "type": "text", "id": "size_prompt", "label": "Size prompt", "default": "Choose your size" },
    { "type": "text", "id": "sizes", "label": "Sizes (comma-separated)", "default": "XS,S,M,L,XL" },

    { "type": "text", "id": "cta_label", "label": "Button text", "default": "Add to cart" }
  ],
  "blocks": [],
  "presets": [
    { "name": "Orange Wide Leg Card" }
  ]
}
{% endschema %}
